{
  "name": "new-react-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "antd": "^4.11.2",
    "axios": "^0.21.1",
    "babel-plugin-import": "^1.13.3",
    "customize-cra": "^1.0.0",
    "js-cookie": "^2.2.1",
    "moment": "^2.29.1",
    "node-sass": "^4.0.0",
    "react": "^17.0.1",
    "react-app-rewired": "^2.1.8",
    "react-dom": "^17.0.1",
    "react-intl": "^5.11.0",
    "react-redux": "^7.2.2",
    "react-router-dom": "^5.2.0",
    "react-scripts": "^4.0.1",
    "redux": "^4.0.5",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "start": "set \"PORT=80\" && react-app-rewired start",
    "build:dev": "set \"GENERATE_SOURCEMAP=true\" && react-app-rewired build dev",
    "build:stg": "set \"GENERATE_SOURCEMAP=false\" && react-app-rewired build stg",
    "build:prod": "set \"GENERATE_SOURCEMAP=false\" && react-app-rewired build prod",
    "build": "set \"GENERATE_SOURCEMAP=false\" && react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
